local component = require("component")
local event = require("event")

local m = component.modem
m.open(888)
print(m.isOpen(888))


meshandler(_, _, from, port, _, message)

print(from)
print(port)
print(message)
--message = tostring(message)
--local command = require ("command/"..message)
--command.do()
end

event.listen("modem_message", meshandler)